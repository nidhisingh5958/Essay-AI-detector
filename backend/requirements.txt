fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7

# NLP / ML (added as Phase 3/4 introduce real usage — pinned loosely for now)
transformers>=4.42
torch>=2.3
spacy>=3.7
wordfreq>=3.1   # rare-word-ratio feature source, see DEC-006
scikit-learn>=1.5
pandas>=2.2
numpy>=1.26

# Dataset acquisition (used by scripts/, not the running backend service —
# see scripts/README.md; kept in this same environment to avoid a second
# virtualenv for a hackathon-scale project)
kaggle>=1.6

# Semantic-preservation screening signal (DEC-012, used by
# scripts/semantic_screen.py) — a SCREEN only, never the detector or a
# ground-truth judge; see DEC-004/DEC-012.
sentence-transformers>=3.0

# Testing
pytest>=8.2
httpx>=0.27
