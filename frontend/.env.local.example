# Copy this file to .env.local (gitignored) and adjust if your backend
# runs somewhere other than the default local development address.
# This is NOT a secret -- NEXT_PUBLIC_ variables are bundled into the
# client-side JavaScript and are visible to anyone using the app.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
